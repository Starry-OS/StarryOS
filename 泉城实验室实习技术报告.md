# 泉城实验室实习技术报告

## 所有任务的完成情况

1. 对现有的 Starry SD 卡驱动设计，在QEMU和开发板上复现已有结果，形成或完善部署和动态跟踪文档；
2. 分析已有 SD 卡驱动，形成或完善 SD 卡代码分析注释和文档；
3. 完善 SD 卡驱动驱动的测试，并修改小的不足；
4. 为现有的 SD 卡驱动（基于轮询）增加了 DMA 读写功能。

### 对现有的 Starry SD 卡驱动设计，在QEMU和开发板上复现已有结果，形成或完善部署和动态跟踪文档

对 Starry 现有的 SD 卡驱动设计的描述和成果复现方案的文档主体被保存在[starry-sdmmc-driver-doc GitHub 仓库](https://github.com/PassingThroughStarDust/starry-sdmmc-driver-doc)。

### 分析已有 SD 卡驱动，形成或完善 SD 卡代码分析注释和文档

目前 Starry 使用的 SD 卡驱动为 [simple-sdmmc](https://github.com/Starry-OS/simple-sdmmc)，对于其代码的解读已经被集成在上述文档中。

### 完善 SD 卡驱动驱动的测试，并修改小的不足

测试相关代码已经放在了[sdmmc-tests GitHub 仓库](https://github.com/PassingThroughStarDust/sdmmc-tests)中。

#### 测试集设计介绍

**测试集源代码结构**：

``` text
sdmmc_tests/
├── src/
│   │── main.rs                # 主机系统本地测试入口
│   ├── lib.rs                 # Starry 集成测试框架入口
│   ├── basic/                 # 基础I/O测试
│   │   ├── basic_io.rs        # 单块/多块读写测试
│   │   ├── edge_cases.rs      # 边界条件测试
│   │   └── error_handling.rs  # 错误处理测试
│   ├── benchmark/             # 性能基准测试
│   │   ├── throughput.rs      # 顺序吞吐量测试
│   │   ├── random_access.rs   # 随机访问性能测试
│   │   └── dma_cpu_mode.rs    # DMA与CPU模式对比
│   ├── concurrency/           # 并发测试
│   │   ├── cache_coherence.rs # 缓存一致性测试
│   │   └── concurrent_stress.rs # 压力测试
│   └── visionfive2/           # VisionFive2平台特定测试
│       ├── sdio_clock_switching.rs # SDIO时钟切换测试
│       ├── interrupt_latency.rs    # 中断延迟测试
│       └── lba48_support.rs        # 48位LBA寻址测试
└── README.md
```

**各类测试通用的核心类型定义**：

```rust
// sdmmc_tests/src/lib.rs

/// 块大小常量（标准SD卡块大小）
pub const BLOCK_SIZE: usize = 512;

/// 测试结果类型
pub type TestResult<T = ()> = Result<T, TestError>;

/// 测试错误类型
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TestError {
    ReadError,        // 块设备读错误
    WriteError,       // 块设备写错误
    DataMismatch,     // 数据校验失败
    InvalidBlock,     // 无效块地址
    Timeout,          // 设备超时
    AssertionFailed,  // 断言失败
    Unsupported,      // 不支持的操作
    IoError,          // 通用I/O错误
}

/// 性能测试统计信息
#[derive(Debug, Default, Clone)]
pub struct TestStats {
    pub bytes_transferred: u64,  // 传输字节数
    pub elapsed_nanos: u64,      // 耗时（纳秒）
    pub operations: u64,         // 操作次数
    pub errors: u64,             // 错误次数
}

impl TestStats {
    /// 计算吞吐量 (MB/s)
    pub fn throughput_mbps(&self) -> f64 {
        if self.elapsed_nanos == 0 {
            return 0.0;
        }
        let bytes = self.bytes_transferred as f64;
        let seconds = self.elapsed_nanos as f64 / 1_000_000_000.0;
        bytes / seconds / (1024.0 * 1024.0)
    }

    /// 计算IOPS
    pub fn iops(&self) -> f64 {
        if self.elapsed_nanos == 0 {
            return 0.0;
        }
        let seconds = self.elapsed_nanos as f64 / 1_000_000_000.0;
        self.operations as f64 / seconds
    }
}

/// 运行所有测试
pub fn run_all_tests() -> TestResult {
    log::info!("=== Starting SD/MMC Driver Tests ===");

    log::info!("--- Running Basic I/O Tests ---");
    basic::run_all_basic_tests()?;

    log::info!("--- Running Benchmark Tests ---");
    benchmark::run_all_benchmarks()?;

    log::info!("--- Running Concurrency Tests ---");
    concurrency::run_all_concurrency_tests()?;

    #[cfg(feature = "vf2")]
    {
        log::info!("--- Running VisionFive2 Specific Tests ---");
        visionfive2::run_all_vf2_tests()?;
    }

    log::info!("=== All SD/MMC Driver Tests Passed ===");
    Ok(())
}
```

#### 测试集的测试种类

| 分类          | 测试文件名               | 主要测试内容         |
|---------------|--------------------------|----------------------|
| 基础功能      | basic_io.rs              | 单/多块读写          |
|               | edge_cases.rs            | 边界块、特殊块操作   |
|               | error_handling.rs        | 错误与异常处理       |
| 性能基准      | throughput.rs            | 顺序吞吐量           |
|               | random_access.rs         | 随机访问IOPS/延迟    |
|               | dma_cpu_mode.rs          | DMA与CPU模式性能对比 |
| 并发一致性    | cache_coherence.rs       | 缓存一致性           |
|               | concurrent_stress.rs     | 并发压力/一致性      |
| VisionFive2   | sdio_clock_switching.rs  | SDIO时钟切换         |
|               | interrupt_latency.rs     | 中断延迟             |
|               | lba48_support.rs         | LBA48大容量支持      |

#### 测试集使用方法

##### 在主机系统上独立运行 `sdmmc-tests`

`sdmmc-test` 允许用户在主机操作系统上通过调用 Rust std 标准库运行，这方便了未来在新增测试样例或者调整测试代码结构时检测效果。

在主机上运行测试只需要开启 `sdmmc-tests` 的 `std` feature 以调用标准库运行。运行案例如下：

``` shell
$ cargo run --features "std"
=============================================
    SD/MMC Driver Test Suite for StarryOS    
=============================================

[1/3] Running Basic I/O Tests...
----------------------------------------------
[INFO  sdmmc_tests::basic] [Basic] Running single block read test...
[INFO  sdmmc_tests::basic] [Basic] Single block read test passed
[INFO  sdmmc_tests::basic] [Basic] Running single block write test...
[INFO  sdmmc_tests::basic] [Basic] Single block write test passed
[INFO  sdmmc_tests::basic] [Basic] Running multi-block read test...
[INFO  sdmmc_tests::basic] [Basic] Multi-block read test passed
[INFO  sdmmc_tests::basic] [Basic] Running multi-block write test...
[INFO  sdmmc_tests::basic] [Basic] Multi-block write test passed
[INFO  sdmmc_tests::basic] [Basic] Running read-write verification test...
[INFO  sdmmc_tests::basic] [Basic] Read-write verification test passed
[INFO  sdmmc_tests::basic] [Basic] Running edge case tests...
[INFO  sdmmc_tests::basic] [Basic] Edge case tests passed
[INFO  sdmmc_tests::basic] [Basic] Running error handling tests...
[INFO  sdmmc_tests::basic] [Basic] Error handling tests passed
[INFO  sdmmc_tests::basic] [Basic] All basic tests completed successfully
[PASS] Basic I/O Tests completed successfully

[2/3] Running Benchmark Tests...
----------------------------------------------
[INFO  sdmmc_tests::benchmark] [Benchmark] Running sequential throughput benchmark...
[INFO  sdmmc_tests::benchmark] [Benchmark] Sequential read: 0.50 MB/s, write: 0.25 MB/s
[INFO  sdmmc_tests::benchmark] [Benchmark] Running random access benchmark...
[INFO  sdmmc_tests::benchmark] [Benchmark] Random read IOPS: 256.00, write IOPS: 128.00
[INFO  sdmmc_tests::benchmark] [Benchmark] Running DMA vs CPU mode comparison...
[INFO  sdmmc_tests::benchmark::dma_cpu_mode] DMA mode: ~10 MB/s read, ~5 MB/s write (estimated)
[INFO  sdmmc_tests::benchmark::dma_cpu_mode] CPU mode: ~2 MB/s read, ~1 MB/s write (estimated)
[INFO  sdmmc_tests::benchmark::dma_cpu_mode] DMA provides ~5x improvement over CPU mode
[INFO  sdmmc_tests::benchmark] [Benchmark] DMA vs CPU comparison completed
[INFO  sdmmc_tests::benchmark] [Benchmark] All benchmarks completed successfully
[PASS] Benchmark Tests completed successfully

[3/3] Running Concurrency Tests...
----------------------------------------------
[INFO  sdmmc_tests::concurrency] [Concurrency] Running cache coherence tests...
[INFO  sdmmc_tests::concurrency] [Concurrency] Cache coherence tests passed
[INFO  sdmmc_tests::concurrency] [Concurrency] Running concurrent stress tests...
[INFO  sdmmc_tests::concurrency] [Concurrency] Concurrent stress tests passed
[INFO  sdmmc_tests::concurrency] [Concurrency] All concurrency tests completed successfully
[PASS] Concurrency Tests completed successfully

=============================================
                 TEST SUMMARY                
=============================================
  Passed: 3
  Failed: 0
  Total:  3

All tests passed!
```

##### 将 `sdmmc-tests` 接入 Starry

按照以下步骤可以在 StarryOS 项目根目录中本地接入 `sdmmc-tests`：

1. 将 `sdmmc-tests` 加入 Starry 的 Workspace 中。

    ``` toml
    # StarryOS/Cargo.toml

    [workspace]
    resolver = "2"
    members = ["kernel", "sdmmc-tests"]
    exclude = ["arceos"]
    ```

2. 将 `sdmmc-tests` 作为 `kernel` crate 的依赖。

    ``` toml
    # StarryOS/kernel/Cargo.toml

    [dependencies]
    sdmmc_tests = { path = "../sdmmc-tests", default-features = false }
    ```

3. 在 `kernel` crate 的 `init` 函数中调用 `sdmmc-tests` 的 `run_all_tests` 函数运行所有测试。

    ``` rust
    //  StarryOS/kernel/src/entry.rs

    pub fn init(args: &[String], envs: &[String]) {
        pseudofs::mount_all().expect("Failed to mount pseudofs");
        spawn_alarm_task();

        if let Err(e) = run_all_tests() {
            panic!("SD/MMC tests failed during kernel init: {:?}", e);
        }

        ...
    }
    ```

**理想情况下的输出如下**：

在 Starry 中，`sdmmc-tests` 的测试结果会输出为 `info` 级别的日志信息。

``` shell
$ make run

...

[  0.584257 0:2 sdmmc_tests:105] === Starting SD/MMC Driver Tests ===
[  0.584540 0:2 sdmmc_tests:107] --- Running Basic I/O Tests ---
[  0.584810 0:2 sdmmc_tests::basic:17] [Basic] Running single block read test...
[  0.585129 0:2 sdmmc_tests::basic:19] [Basic] Single block read test passed
[  0.585422 0:2 sdmmc_tests::basic:21] [Basic] Running single block write test...
[  0.585755 0:2 sdmmc_tests::basic:23] [Basic] Single block write test passed
[  0.586062 0:2 sdmmc_tests::basic:25] [Basic] Running multi-block read test...
[  0.586386 0:2 sdmmc_tests::basic:27] [Basic] Multi-block read test passed
[  0.586684 0:2 sdmmc_tests::basic:29] [Basic] Running multi-block write test...
[  0.587010 0:2 sdmmc_tests::basic:31] [Basic] Multi-block write test passed
[  0.587310 0:2 sdmmc_tests::basic:33] [Basic] Running read-write verification test...
[  0.587673 0:2 sdmmc_tests::basic:35] [Basic] Read-write verification test passed
[  0.587988 0:2 sdmmc_tests::basic:37] [Basic] Running edge case tests...
[  0.588295 0:2 sdmmc_tests::basic:39] [Basic] Edge case tests passed
[  0.588577 0:2 sdmmc_tests::basic:41] [Basic] Running error handling tests...
[  0.588902 0:2 sdmmc_tests::basic:43] [Basic] Error handling tests passed
[  0.589188 0:2 sdmmc_tests::basic:45] [Basic] All basic tests completed successfully
[  0.589524 0:2 sdmmc_tests:110] --- Running Benchmark Tests ---
[  0.589852 0:2 sdmmc_tests::benchmark:17] [Benchmark] Running sequential throughput benchmark...
[  0.590234 0:2 sdmmc_tests::benchmark:19] [Benchmark] Sequential read: 0.50 MB/s, write: 0.25 MB/s
[  0.591364 0:2 sdmmc_tests::benchmark:25] [Benchmark] Running random access benchmark...
[  0.591723 0:2 sdmmc_tests::benchmark:27] [Benchmark] Random read IOPS: 256.00, write IOPS: 128.00
[  0.592131 0:2 sdmmc_tests::benchmark:33] [Benchmark] Running DMA vs CPU mode comparison...
[  0.592481 0:2 sdmmc_tests::benchmark::dma_cpu_mode:51] DMA mode: ~10 MB/s read, ~5 MB/s write (estimated)
[  0.592884 0:2 sdmmc_tests::benchmark::dma_cpu_mode:52] CPU mode: ~2 MB/s read, ~1 MB/s write (estimated)
[  0.593269 0:2 sdmmc_tests::benchmark::dma_cpu_mode:53] DMA provides ~5x improvement over CPU mode
[  0.593646 0:2 sdmmc_tests::benchmark:35] [Benchmark] DMA vs CPU comparison completed
[  0.593983 0:2 sdmmc_tests::benchmark:37] [Benchmark] All benchmarks completed successfully
[  0.594341 0:2 sdmmc_tests:113] --- Running Concurrency Tests ---
[  0.594696 0:2 sdmmc_tests::concurrency:15] [Concurrency] Running cache coherence tests...
[  0.595126 0:2 sdmmc_tests::concurrency:17] [Concurrency] Cache coherence tests passed
[  0.595550 0:2 sdmmc_tests::concurrency:19] [Concurrency] Running concurrent stress tests...
[  0.596059 0:2 sdmmc_tests::concurrency:21] [Concurrency] Concurrent stress tests passed
[  0.596546 0:2 sdmmc_tests::concurrency:23] [Concurrency] All concurrency tests completed successfully
[  0.597051 0:2 sdmmc_tests:122] === All SD/MMC Driver Tests Passed ===
```

### 为现有的 SD 卡驱动（基于轮询）增加了 DMA 读写功能

修改后的代码放在[simple-sdmmc-extended GitHub 仓库](https://github.com/PassingThroughStarDust/simple-sdmmc-extended)中。

#### 驱动 DMA 功能介绍

1. IDMAC 初始化流程

   1. **分配并初始化描述符环（Descriptor Ring）**
      - 分配一块物理连续的内存作为 DMA 描述符环（根据 Starry 的设计，代码中只用一个描述符），并初始化所有描述符的链表关系。
      - 设置每个描述符的下一个描述符地址，最后一个描述符设置为环形（End-of-Ring）。

   2. **IDMAC 控制器复位**
      - 通过写 BMOD（Bus Mode Register）和 CTRL（Control Register）相关位，软复位 DMA 控制器。

   3. **配置中断**
      - 屏蔽所有中断，仅使能必要的 DMA 传输完成（NI, RI, TI）中断。

   4. **设置描述符基地址**
      - 将描述符环的物理首地址写入 DBADDR（Descriptor List Base Address Register）。

2. DMA 方式读写 SD 卡流程

   1. **准备数据传输参数**
      - 设置块大小、传输字节数等参数。
      - 填写描述符内容，包括数据缓冲区物理地址、长度、控制位（OWN、CH、FD、LD 等）。

   2. **启动 DMA 传输**
      - 选择 IDMAC 接口（CTRL 寄存器 USE_INTERNAL_DMAC 位置 1）。
      - 使能 IDMAC（BMOD 寄存器 DE 位置 1）。
      - 向 PLDMND（Poll Demand Register）写任意值，触发 FSM 启动。

   3. **等待传输完成**
      - 通过中断或轮询方式等待 DMA 传输完成。

   4. **停止/复位 DMA**
      - 清理描述符、状态等。

#### 基于 DW_MSHC IDMAC 控制器的 DMA 读写代码介绍

**接口定义**：

1. 原有 SDIO 控制器结构体抽象的数据结构扩展

    ``` rust
    pub struct SdMmc {
        /// Register block for the SD/MMC controller, accessed through volatile reads/writes.
        regs: VolatilePtr<'static, RegisterBlock>,

        /// Number of blocks on the SD/MMC card, determined during initialization from the CSD register.
        num_blocks: u64,

        dma_enabled: bool,
    }
    ```

2. 基于 IDMAC 的 DMA 读写接口函数

    `SdMmc` 具有以下接口函数以启用 IDMAC 进行并进行数据块的读取：
    - **init**
      - 初始化 SD/MMC 控制器与卡状态机，完成驱动启动时的全流程配置。
      - 主要步骤包括：清理中断状态、配置并稳定时钟、上电与总线宽度设置、复位 DMA 相关状态、卡初始化命令序列（CMD0/CMD8/ACMD41/CMD2/CMD3/CMD9/CMD7 等）。
      - 在初始化末尾读取 SCR 并调用 `enable_idmac` 打开 IDMAC 能力，为后续 DMA 数据传输做准备。

    - **send_cmd_idmac**
      - 发送带数据阶段的 SD 命令，并在需要时使用 IDMAC 完成数据搬运。
      - 在发命令前清理陈旧中断状态；若有数据传输则构造单个 `IdmacDescriptor`，填充缓冲区物理地址与长度，写入 `DBADDR` 并通过 `PLDMND` 触发 DMA。
      - 轮询命令响应与数据完成状态，结合 `RINTSTS` 与 `IDSTS` 判断错误，成功返回响应寄存器内容，失败返回 None。

    - **enable_idmac**
      - 使能控制器内部 DMA（IDMAC）通道。
      - 通过 BMOD 设置 `DE`/`FB`/`DSL` 等关键位，并在 CTRL 中置位 USE_INTERNAL_DMAC，让数据路径切换到 IDMAC。
      - 同步更新驱动内部 dma_enabled 状态，表示后续读写可走 DMA 流程。

    - **read_block**
      - 读取单个 512 字节数据块。
      - 先设置块大小与传输字节数（`set_transaction_size(512, 512)`），再调用 `send_cmd_idmac(Command::ReadSingleBlock(...))` 发起 DMA 读。
      - 读流程完成后保留 FIFO/状态跟踪日志，供调试传输行为使用。

    - **write_block**
      - 写入单个 512 字节数据块。
      - 同样先配置传输尺寸（512 字节），再调用 `send_cmd_idmac(Command::WriteSingleBlock(...))` 发起 DMA 写。
      - 写完成后记录 FIFO 状态，用于观测发送路径与控制器状态变化。

    ``` rust
    impl SdMmc {

        ...

        /// Sends a command using the Internal DMA (IDMAC) for data transfer if required.
        pub fn send_cmd_idmac(
            &self,
            command: Command<'_>,
        ) -> Option<[u32; 4]> {
            trace!("send_cmd_idmac {command:#x?}");

            let (cmd, arg, xfer) = command.build();
            assert_eq!(cmd.data_expected(), xfer.is_some());

            trace!("send_cmd_idmac {cmd:?} {arg:#x?}");

            wait_until(|| self.can_send_cmd());
            if cmd.data_expected() {
                wait_until(|| self.can_send_data());
            }

            // Clear stale status before a new command/DMA transaction.
            let stale_rintsts = self.regs.rintsts().read();
            self.regs.rintsts().write(stale_rintsts);
            let idsts_before = self.regs.idsts().read();

            if let Some(xfer) = xfer {
                info!("Data required, using IDMAC for transfer");

                let (buf_len, buf_ptr) = match xfer {
                    DataXfer::Read(buf) => {
                        let len = buf.len();
                        (len, buf.as_ptr() as usize)
                    }
                    DataXfer::Write(buf) => {
                        let len = buf.len();
                        (len, buf.as_ptr() as usize)
                    }
                };

                assert!(
                    buf_len <= 0x1fff,
                    "IDMAC single descriptor buffer too large: {buf_len}"
                );

                // Convert the virtual address of the buffer to a physical address for DMA.
                let buf_phys = buf_ptr - Self::KERNEL_VIRT_PHYS_OFFSET;
                trace!("Buffer physical address: 0x{:08x}", buf_phys);

                // Set up the IDMAC descriptor for the DMA transfer.
                // Use one descriptor for one contiguous buffer.
                let mut desc = IdmacDescriptor::new();
                // Set the control bits for the DMA transfer in des0.
                // OWN must be set so IDMAC can fetch and process the descriptor.
                desc.set_desc0_control_descriptor(true, false, false, false, true, true, false);
                desc.set_des1_buffer1_size(buf_len as u16);
                desc.set_des2_buffer1_address(buf_phys as u32);
                desc.set_des3_next_descriptor_address(0);

                // Write the physical address of the descriptor to the DBADDR register to set up the DMA transfer.
                let desc_addr = (&desc as *const IdmacDescriptor) as usize - Self::KERNEL_VIRT_PHYS_OFFSET;
                // Ensure descriptor writes are visible before giving its address to IDMAC.
                fence(Ordering::Release);
                self.regs.dbaddr().write(desc_addr as u32);
                self.regs.pldmnd().write(1);
                trace!("IDMAC descriptor set up at physical address: 0x{:08x}", desc_addr);
            }

            // Write the command argument and command index to the CMDARG and CMD registers to send the command.
            self.regs.cmdarg().write(arg);
            self.regs.cmd().write(cmd);

            trace!("cmd {} sent", cmd.cmd_index());

            // Wait for the command to be sent and the response to be received, checking for errors.
            if cmd.response_expect() {
                wait_until(|| self.has_response());
                trace!("cmd {} received response", cmd.cmd_index());
            }

            // If data transfer is expected, wait for the transfer to complete, checking for errors.
            if cmd.data_expected() {
                wait_until(|| {
                    let rintsts = self.regs.rintsts().read();
                    let idsts = self.regs.idsts().read();
                    let idmac_new_error =
                        (!idsts_before.fbe() && idsts.fbe())
                            || (!idsts_before.du() && idsts.du())
                            || (!idsts_before.ces() && idsts.ces());

                    rintsts.data_transfer_over()
                        || rintsts.error()
                        || idmac_new_error
                });
            }

            // Read response and check for errors after sending the command and setting up DMA.
            let resp = self.regs.resp().read();

            // Read the interrupt status to check for errors and clear the status bits.
            let rintsts = self.regs.rintsts().read();
            let idsts = self.regs.idsts().read();
            let idmac_new_error =
                (!idsts_before.fbe() && idsts.fbe())
                    || (!idsts_before.du() && idsts.du())
                    || (!idsts_before.ces() && idsts.ces());
            // clear interrupt status
            self.regs.rintsts().write(rintsts);

            if rintsts.error() || idmac_new_error {
                trace!(
                    "cmd {} error: rintsts={rintsts:?} idsts={idsts:?} resp={resp:?}",
                    cmd.cmd_index()
                );
                return None;
            }

            Some(resp)
        }

        fn init(&mut self) {
            info!("Initializing SD/MMC driver at {:?}", self.regs);

            // On VisionFive2, some registers have been initialized by the bootloader(U-Boot).
            // But some default values are not suitable for our driver, so we need to reset and reconfigure them.
            
            ...

            // Clear any stale interrupt status flags left by bootloader
            // Writing 1 to these bits clears them
            let rintsts = self.regs.rintsts().read();
            trace!("initial rintsts: {rintsts:?}");
            self.regs.rintsts().write(rintsts);
            trace!("cleared interrupt status");

            // Clock is already initialized by U-Boot, but we need to reconfigure it
            // First, check current state
            warn!("=== SD/MMC Clock Initialization ===");
            warn!("Initial clkena: {:?}", self.regs.clkena().read());
            warn!("Initial clkdiv: {:?}", self.regs.clkdiv().read());
            warn!("Initial ctrl: {:?}", self.regs.ctrl().read());

            // Disable clock for configuration
            warn!("Step 1: Disabling clock...");
            self.regs.clkena().write(ClkEna::new());
            warn!("  clkena after disable: {:?}", self.regs.clkena().read());
            
            // Send ResetClock command to update clock in disabled state
            warn!("Step 2: Sending ResetClock in disabled state...");
            match self.send_cmd(Command::ResetClock) {
                Some(_) => warn!("  ResetClock succeeded"),
                None => warn!("  ResetClock FAILED - continuing anyway"),
            }

            // Set clock divider to lower frequency (slower for compatibility)
            warn!("Step 3: Setting clock divider to 100 (lower frequency)...");
            self.regs.clkdiv().write(ClkDiv::new().with_clk_divider0(100));
            warn!("  clkdiv after set: {:?}", self.regs.clkdiv().read());

            // Now enable clock with new divider
            warn!("Step 4: Enabling clock...");
            self.regs.clkena().write(ClkEna::new().with_cclk_enable(1));
            warn!("  clkena after enable: {:?}", self.regs.clkena().read());
            
            // Send ResetClock to activate new clock settings
            warn!("Step 5: Sending ResetClock to activate new clock...");
            match self.send_cmd(Command::ResetClock) {
                Some(_) => warn!("  ResetClock succeeded"),
                None => warn!("  ResetClock FAILED - continuing anyway"),
            }
            
            // Long delay to let everything stabilize
            warn!("Step 6: Waiting for clock stabilization...");
            for _ in 0..10000 {
                core::hint::spin_loop();
            }

            ...

            // Check card presence and status
            warn!("=== Pre-Command Diagnostics ===");
            warn!("CTYPE (card type): {:?}", self.regs.ctype().read());
            warn!("STATUS register: {:?}", self.regs.status().read());
            let status = self.regs.status().read();
            warn!("  card_data_3_status: {}", status.data_3_status());
            warn!("  fifo_count: {}", status.fifo_count());
            warn!("  command_fsm_states: {}", status.command_fsm_states());
            warn!("RINTSTS (interrupt status): {:?}", self.regs.rintsts().read());
            warn!("MINTSTS (masked interrupt): {:?}", self.regs.mintsts().read());
            
            // Enable card power if available in PWREN register
            warn!("Setting card power enable (PWREN)...");
            self.regs.pwren().write(1u32.into());  // Card power enable
            warn!("  PWREN: {:?}", self.regs.pwren().read());
            
            // Increased stabilization delay
            warn!("Extended clock stabilization delay (100k cycles)...");
            for _ in 0..100000 {
                core::hint::spin_loop();
            }
            
            // set data width -> 1bit
            self.regs.ctype().write(0.into());

            // reset dma
            self.regs.bmod().update(|r| r.with_de(false).with_swr(true));
            self.regs
                .ctrl()
                .update(|r| r.with_dma_reset(true).with_use_internal_dmac(false));

            trace!("dma reset");

            trace!("ctrl: {:?}", self.regs.ctrl().read());

            warn!("=== Sending GoIdleState command ===");
            warn!("  Before GoIdleState - STATUS: {:?}", self.regs.status().read());
            warn!("  Before GoIdleState - RINTSTS: {:?}", self.regs.rintsts().read());
            // Note: GoIdleState may timeout during initial card detection phase.
            // This is not fatal - the card responds to SendIfCond and continues initialization normally.
            // The timeout likely occurs because the card is still stabilizing at the new clock frequency.
            match self.send_cmd(Command::GoIdleState) {
                Some(_) => warn!("GoIdleState succeeded"),
                None => warn!("GoIdleState timeout (expected during initialization) - continuing..."),
            }
            trace!("idle state set");

            warn!("Sending SendIfCond command...");
            let has_valid_resp = match self.send_cmd(Command::SendIfCond(0x1aa)) {
                Some(resp) => {
                    warn!("SendIfCond succeeded: {:?}", resp);
                    if resp[0] & 0xff != 0xaa {
                        warn!("Warning: unexpected response for SendIfCond");
                        false
                    } else {
                        true
                    }
                }
                None => {
                    warn!("SendIfCond FAILED - card not responding or unsupported");
                    false
                }
            };
            
            if !has_valid_resp {
                warn!("SD card not responding properly - continuing anyway");
            }

            warn!("Starting ACMD41 loop to detect SD card...");
            let mut attempt = 0;
            let mut card_initialized = false;
            loop {
                attempt += 1;
                if attempt > 100 {
                    warn!("ACMD41 loop exceeded 100 attempts - giving up");
                    break;
                }
                
                match self.send_cmd(Command::AppCmd(0)) {
                    Some(_) => trace!("AppCmd succeeded"),
                    None => {
                        warn!("AppCmd failed on attempt {}", attempt);
                        continue;
                    }
                }
                
                match self.send_cmd(Command::SdSendOpCond(0x41FF_8000)) {
                    Some(resp) => {
                        let ocr = resp[0];
                        if ocr & 0x8000_0000 != 0 {
                            warn!("SD card is ready after {} attempts", attempt);
                            card_initialized = true;
                            if ocr & 0x4000_0000 != 0 {
                                debug!("SD card supports high capacity");
                            } else {
                                debug!("SD card is standard capacity");
                            }
                            break;
                        } else {
                            trace!("SD card not ready yet, attempt {}, ocr: {ocr:x}", attempt);
                        }
                    }
                    None => {
                        warn!("SdSendOpCond failed on attempt {}", attempt);
                    }
                }
                
                core::hint::spin_loop();
            }
            
            if !card_initialized {
                warn!("Card initialization failed - continuing anyway");
                return;  // Cannot continue without card
            }

            warn!("Sending AllSendCid command...");
            match self.send_cmd(Command::AllSendCid) {
                Some(resp) => {
                    let cid = unsafe { core::mem::transmute::<[u32; 4], Cid>(resp) };
                    warn!("cid: {cid:?}");
                }
                None => {
                    warn!("AllSendCid failed - cannot determine card ID");
                    return;
                }
            }

            warn!("Sending SendRelativeAddr command...");
            let rca = match self.send_cmd(Command::SendRelativeAddr) {
                Some(resp) => {
                    let rca = (resp[0] >> 16) & 0xffff;
                    debug!("rca: {rca:#x}");
                    rca
                }
                None => {
                    warn!("SendRelativeAddr failed - cannot get card address");
                    return;
                }
            };

            warn!("Sending SendCsd command...");
            match self.send_cmd(Command::SendCsd(rca << 16)) {
                Some(resp) => {
                    let csd = unsafe { core::mem::transmute::<[u32; 4], CsdV2>(resp) };
                    debug!("csd: {csd:?}");
                    self.num_blocks = csd.num_blocks();
                    warn!("SD card capacity: {:#x} blocks", self.num_blocks);
                }
                None => {
                    warn!("SendCsd failed - cannot determine card capacity");
                    self.num_blocks = 0;
                }
            }

            warn!("Sending SelectCard command...");
            match self.send_cmd(Command::SelectCard(rca << 16)) {
                Some(_) => warn!("SelectCard succeeded"),
                None => warn!("SelectCard failed"),
            }

            warn!("Sending AppCmd command...");
            match self.send_cmd(Command::AppCmd(rca << 16)) {
                Some(_) => warn!("AppCmd succeeded"),
                None => warn!("AppCmd failed"),
            }

            // Read SCR register of SD card to determine supported bus widths.
            // This is needed before we can set the bus width.
            self.set_transaction_size(8, 8);
            // Although the SCR register is only 8 bytes, we allocate a 512-byte buffer here.
            // This is because many controllers and drivers require the buffer to be block-aligned (e.g., 512 bytes),
            // and only the first 8 bytes will be filled with valid data. The rest is ignored.
            // This ensures compatibility with hardware and avoids DMA or alignment issues.
            let mut buf = [0u8; 512];
            warn!("Sending SendScr command...");
            match self.send_cmd(Command::SendScr(&mut buf)) {
                Some(_) => warn!("SendScr succeeded"),
                None => warn!("SendScr failed"),
            }

            trace!("fifo count: {}", self.fifo_cnt());
            let resp = unsafe {
                self.regs
                    .as_raw_ptr()
                    .byte_add(Self::FIFO)
                    .cast::<u64>()
                    .read_volatile()
            };
            debug!("Bus width supported: {:#x?}", (resp >> 8) & 0xf);
            trace!("fifo count: {}", self.fifo_cnt());

            // Enable IDMAC for DMA transfer.
            self.enable_idmac();

            trace!("ctrl: {:?}", self.regs.ctrl().read());
            let rintsts = self.regs.rintsts().read();
            trace!("rintsts: {rintsts:?}");
            self.regs.rintsts().write(rintsts); // clear interrupt status

            info!("SD/MMC driver initialized");
        }

        /// Reads a single block from the SD/MMC card.
        pub fn read_block(&mut self, block: u32, buf: &mut [u8; 512]) {
            self.set_transaction_size(512, 512);
            // self.send_cmd(Command::ReadSingleBlock(block, buf)).unwrap();
            self.send_cmd_idmac(Command::ReadSingleBlock(block, buf)).unwrap();
            trace!("fifo count: {}", self.fifo_cnt());
        }

        /// Writes a single block to the SD/MMC card.
        pub fn write_block(&mut self, block: u32, buf: &[u8; 512]) {
            self.set_transaction_size(512, 512);
            // self.send_cmd(Command::WriteSingleBlock(block, buf)).unwrap();
            self.send_cmd_idmac(Command::WriteSingleBlock(block, buf)).unwrap();
            trace!("fifo count: {}", self.fifo_cnt());
        }

        /// Enables the Internal DMA (IDMAC) for DMA transfers.
        pub fn enable_idmac(&mut self) {
            info!("Enabling IDMAC for DMA transfer");

            // reset part for reference
            //  self.regs.bmod().update(|r| r.with_de(false).with_swr(true));
            // self.regs
            //    .ctrl()
            //    .update(|r| r.with_dma_reset(true).with_use_internal_dmac(false));

            // Set the BMOD register to enable the internal DMA controller (IDMAC).
            // BMOD's PBL value is read-only value and is the mirror of MSIZE of FIFOTH register.
            // And the DSL value is applicable only for dual buffer structure.
            self.regs.bmod().update(|r| r.with_de(true).with_dsl(0).with_fb(true));
            
            // TODO: Enable interrupts.

            // Set the CTRL register to enable the use of the internal DMA controller (IDMAC).
            self.regs.ctrl().update(|r| r.with_use_internal_dmac(true));

            self.dma_enabled = true;

            info!("IDMAC enabled for DMA transfer");
        }

        ...
    }
    ```

3. IDMAC 描述符链数据结构

    ``` rust
    #[repr(C, align(4))]
    pub struct IdmacDescriptor {
        pub des0: IdmacDes0,
        pub des1: IdmacDes1,
        pub des2: u32,
        pub des3: u32,
    }

    /// IDMAC Descriptor Flags
    #[bitfield(u32, order = Msb)]
    pub struct IdmacDes0 {
        pub own: bool,
        pub ces: bool,

        #[bits(24)]
        pub _reserved1: u32,

        pub er: bool,
        pub ch: bool,
        pub fs: bool,
        pub ld: bool,
        pub dic: bool,

        // Reserved bit (bit 0)
        #[bits(1)]
        pub _reserved0: u8,
    }

    #[bitfield(u32, order = Msb)]
    pub struct IdmacDes1 {
        #[bits(19)]
        pub _reserved: u32,

        #[bits(13)]
        pub bs1: u16,
    }

    impl IdmacDescriptor {
        pub fn new() -> Self {
            trace!("Creating a new IDMAC Descriptor with default values");
            Self {
                des0: IdmacDes0::default(),
                des1: IdmacDes1::default(),
                des2: 0,
                des3: 0,
            }
        }

        /// Sets the control bits for the DMA transfer in des0.
        pub fn set_control_descriptor(&mut self, des0: IdmacDes0) {
            trace!(
                "Setting control descriptor:\nown={}, ces={}, er={}, ch={}, fs={}, ld={}, dic={}",
                des0.own(), des0.ces(), des0.er(), des0.ch(), des0.fs(), des0.ld(), des0.dic()
            );

            self.des0 = des0;
        }

        pub fn set_buffer1_size(&mut self, size: u16) {
            trace!("Setting buffer1 size: {}", size);

            // size.le()
            self.des1.set_bs1(size);
        }

        /// Sets the address of the first data buffer for the DMA transfer in des2.
        pub fn set_buffer1_address(&mut self, addr: u32) {
            trace!("Setting buffer1 address: 0x{:08x}", addr);

            self.des2 = addr;
        }

        pub fn set_next_descriptor_address(&mut self, addr: u32) {
            trace!("Setting next descriptor address: 0x{:08x}", addr);

            self.des3 = addr;
        }
    }
    ```

## 后续安排

原定计划是为 Starry 引入 Rust 异步机制以增强读取 SD 卡时的异步性能这其中包括为驱动扩展 DMA 能力和添加 Rust 异步代码，但由于 DMA 部分卡了太长时间，所以异步没有完全实现功能。后续我会参加开源毕设将异步部分完成。
